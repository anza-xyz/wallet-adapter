import React, { StrictMode } from 'react';
import ReactDOM from 'react-dom';
import { App } from './App';

// A Comment to create a pull request

ReactDOM.render(
    <StrictMode>
        <App />
    </StrictMode>,
    document.getElementById('app')
);
